[English](https://github.com/sagehou/M365-MCP-Server/blob/v0.1.0/docs/releases/v0.1.0.md) | **简体中文**

# M365 MCP Server v0.1.0

项目源代码和文档按 [MIT 许可证](../../../LICENSE)发布。第三方依赖仍适用各自的许可证。

首个稳定版本是一个面向 WorkBuddy 兼容客户端和其他 Streamable HTTP MCP Client
的自托管 Outlook Mail MCP Gateway。它有意保持较小的工具面，并将 Microsoft
Graph 访问限制在当前登录用户的委托 `/me` 邮箱内。

## 包含能力

- Microsoft Entra Bearer Token 校验、Tenant Allowlist 和 Required Scope 约束。
- 通过 On-Behalf-Of（OBO）访问 Microsoft Graph 委托权限。
- OAuth Protected Resource 与 Authorization Server Discovery。
- 可选的 Dynamic Public-client Registration、带 S256 PKCE 的 Interactive
  Authorization Code Flow、加密持久 Session、Refresh-token Rotation、Replay
  Rejection 和重启恢复。
- 每个请求独立的用户身份隔离和安全结构化 Audit Event。
- WorkBuddy Connector Package 及四个限定范围的 Skill。
- 使用 Non-root Runtime 与 Health Check 的 Docker / Docker Compose 部署。
- Windows x64 NativeAOT 本地 stdio 单文件版本，并随 Release 发布 SHA-256 校验文件和 GitHub Build Provenance Attestation。

## Outlook Mail 工具

- `mail_search`
- `mail_get`
- `mail_create_draft`
- `mail_send_draft`
- `mail_list_attachments`
- `mail_read_attachment`
- `mail_download_attachment`
- `mail_mark_read`
- `mail_archive`
- `mail_move`
- `mail_set_category`

附件提取支持 PDF、DOCX、XLSX、PPTX、文本和受保护的压缩包检查。Parser Process
具有 Byte、Archive Expansion、Resource、Timeout 和 Cancellation 限制。下载
Ticket 为短时且只能使用一次。

## Container Image

Release Workflow 会把同一个已经通过 Smoke Test 的 Linux x64 Image 发布为：

- `ghcr.io/sagehou/m365-mcp-server:v0.1.0`
- `ghcr.io/sagehou/m365-mcp-server:0.1.0`
- `ghcr.io/sagehou/m365-mcp-server:0.1`
- `ghcr.io/sagehou/m365-mcp-server:latest`

```bash
docker pull ghcr.io/sagehou/m365-mcp-server:v0.1.0
```

本版本支持 `linux/amd64`，不声明提供 ARM64 Image。

## Windows EXE

同一 Tag Workflow 会构建并 Smoke Test 最终用于发布的 Windows x64 NativeAOT 候选文件，并发布：

- `m365-mcp-windows-x64.exe`
- `m365-mcp-windows-x64.exe.sha256`
- `LICENSE`（法律声明文件，运行单文件 EXE 时不需要）

v0.1.0 的 EXE 不包含 Authenticode 签名。Release 会同时提供 SHA-256 校验文件，并通过 GitHub Artifact Attestation 为该 EXE 记录基于 Sigstore 的构建来源证明。可使用 `gh attestation verify m365-mcp-windows-x64.exe --repo sagehou/M365-MCP-Server` 验证来源。

为便于按精确文件哈希加入杀毒软件白名单，GitHub Release 在线说明还列出已发布
EXE 的 MD5 和 SHA-1。GitHub Actions 对同一 Release 文件计算这些值，更新说明前
会核对其 SHA-256。MD5 和 SHA-1 仅用于兼容旧式白名单表单，不证明文件可信，也
不能取代 SHA-256 与构建来源证明。EXE 一旦变化，就需要重新审核白名单。

## 发布门禁

创建 `v0.1.0` Tag 前，维护者必须完成并记录
[发布检查清单](../release-checklist.md)，包括真实 WorkBuddy 登录与 Refresh、重启
恢复、双用户隔离、跨租户隔离以及代表性真实邮箱附件测试。Mock CI Coverage 不能
替代这些证据。

受限自动发送契约仍包含在本版本中，但符合策略的发送及越界阻止尚未完成真实验收。
启用自动发送前必须在具体部署中完成这两项检查；本 Release 不宣称它们已经通过。

Tag Workflow 会验证 Tag 与 `project.version` 一致、提交属于经过 Review 的
`main` 历史、存在双语 Release Notes，并依次通过完整 Python Test、Production
Image Build、精确 Image Smoke Test、Windows EXE 构建与 Smoke Test、Checksum 校验、Build Provenance Attestation 和 Compose 校验；之后推送所有 Image Tag，最后创建包含 Windows Assets 的 GitHub Release。

## 已知限制

- Search 只返回一个有边界的 Page 和 `next_link` Hint；尚未实现 Opaque
  Continuation Cursor。
- 尚未包含 Folder Discovery、Reply、Forward、HTML/附件撰写、Shared Mailbox、
  OCR、RBAC、Rate Limiting 和 Observability。
- 发送采用 Draft-first 且仅支持纯文本，必须逐封确认或具有明确的受限自动化
  授权；Graph 接受请求不代表已投递，结果不明确的发送失败不会自动重试。
- 受限自动发送由 Client/Agent 层执行授权策略，本版本尚未完成该路径的真实测试。
  具体部署启用前，须验证一次符合策略的发送和一次越界阻止。
- 尚未包含 Calendar、OneDrive、SharePoint 和 Teams。
- OAuth 仍需显式配置；具体部署在完成自己的真实 Client 验收前不应启用。
- 不支持 Multi-replica；临时 Download Ticket 和 SQLite OAuth 状态假设只有一个
  Server Instance。
- Windows 本地 stdio Runtime 已作为未签名的 x64 NativeAOT 单文件纳入 v0.1.0；本地模式提供 10 个 Mail Tool，暂不包含 `mail_download_attachment` 和 PDF/Office 富文档附件解析。

部署前请阅读[部署清单](../deployment.md)、[WorkBuddy OAuth 指南](../workbuddy-oauth.md)
和[实现复核状态](../review-status.md)。
